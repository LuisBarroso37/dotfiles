-- Disabled because mini.surround owns the `s` prefix here (see mini-surround.lua):
-- flash's default `s`/`S` jump motions would shadow sa/sd/sf/sr/sh/sn.
return {
  "folke/flash.nvim",
  enabled = false,
}
